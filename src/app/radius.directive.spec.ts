import { RadiusDirective } from './radius.directive';

describe('RadiusDirective', () => {
  it('should create an instance', () => {
    const directive = new RadiusDirective();
    expect(directive).toBeTruthy();
  });
});
